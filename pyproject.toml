[project]
name = "harbin-route-planner"
version = "0.1.0"
description = "基于Transformer的哈尔滨文旅线路优化模型"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py310"
